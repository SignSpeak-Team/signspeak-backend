# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.30.0

# HTTP Client (para comunicarse con otros servicios)
httpx>=0.27.0

# Validation
pydantic>=2.10.0
pydantic-settings>=2.5.0

# Environment variables
python-dotenv==1.0.0

# Authentication (lo usaremos después)
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4

# Rate Limiting (lo usaremos después)
slowapi>=0.1.9

