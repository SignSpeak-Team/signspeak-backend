[pytest]
testpaths = tests

addopts =
    --strict-markers
    --tb=short
    -p no:warnings
    --show-capture=no
markers =
    unit: Unit tests
    integration: Integration tests
    slow: Slow tests (skipped in CI)
    vision: Vision service tests